{
  "name": "kac-translator-overlay",
  "version": "1.0.0",
  "description": "KAC Translator 줌 오버레이 (투명·항상위 자막 창)",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dist": "electron-packager . KAC-Translator-Overlay --platform=win32 --arch=x64 --out=dist --overwrite"
  },
  "devDependencies": {
    "@electron/packager": "^18.3.6",
    "electron": "^33.2.0"
  }
}
